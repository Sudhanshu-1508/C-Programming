#include<stdio.h>

int Check(iValue)
{
    if(iValue % 4 == o)
    {
        printf("Given year is a Leap Year");
    }
    printf("It is not a leap year");

    return ;
}
int main()

{
    int iYear = 0;

    printf("enter a year");
    scanf("%d", &iYear);

    Ans = check(iYear);
    printf("the result is",%d);
    return0;
}